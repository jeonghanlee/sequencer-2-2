To run this example on a host IOC:
   cd <top>/test/demo
    ../../bin/$EPICS_HOST_ARCH/demo stcmd.host

To run this on vxWorks set startup script to <top>/test/demo/st.cmd

Also st.cmd is loading for vxWorks-68040. You may have to change st.cmd
